@using Models
@model DeliveryViewModel

<div class="card">
    <h5 class="card-header"><span class="card-header-custom">@Localizer["Delivery Submission"]</span><br></h5>
    <div class="card-body">
        <input type="hidden" asp-for="@Model.Id" name="Id" />
        <partial name="_EditDeliveryDetails" model="Model" />        
    </div>
</div>
