@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Report Detail List"];
    ViewData["ActiveMainPage"] = "ReportDetail";
    ViewData["Level1"] = "SQLReportAutoSender";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="reportDetail" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["ReportId"]
							</th>
							<th>
								@Localizer["ReportDetailNumber"]
							</th>
							<th>
								@Localizer["Description"]
							</th>
							<th>
								@Localizer["ConnectionString"]
							</th>
							<th>
								@Localizer["QueryString"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#reportDetail').DataTable({
            select: false,
            serverSide: true,
            order: [[ 5, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/ReportDetail/Index", new { area = "SQLReportAutoSender", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "reportId", name: "ReportId", responsivePriority: 1, title: '@Localizer["ReportId"]', searchable: false },
				{ data: "reportDetailNumber", name: "ReportDetailNumber", responsivePriority: 1, title: '@Localizer["ReportDetailNumber"]', searchable: false },
				{ data: "description", name: "Description", responsivePriority: 1, title: '@Localizer["Description"]', searchable: true },
				{ data: "connectionString", name: "ConnectionString", responsivePriority: 1, title: '@Localizer["ConnectionString"]', searchable: false },
				{ data: "queryString", name: "QueryString", responsivePriority: 1, title: '@Localizer["QueryString"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/ReportDetail/Details", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btn-secondary btnViewReportDetail">@Localizer["View"]</a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportDetail.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ReportDetail/Edit", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary btnEditReportDetail">@Localizer["Edit"]</a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportDetail.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ReportDetail/Delete", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-danger btnDeleteReportDetail">@Localizer["Delete"]</a>';
                    </text>
                }
				   
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportDetail.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/ReportDetail/Add", new { area = "SQLReportAutoSender" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#reportDetail tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddReportDetail",".btnEditReportDetail",".btnViewReportDetail",".btnDeleteReportDetail"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddReportDetail",".btnEditReportDetail",".btnViewReportDetail",".btnDeleteReportDetail"]);
    });
</script>
}