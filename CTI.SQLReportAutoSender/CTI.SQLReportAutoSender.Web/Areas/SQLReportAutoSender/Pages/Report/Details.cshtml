@page
@model DetailsModel
@{
    ViewData["Title"] = Localizer["View Report"];
    ViewData["ActiveMainPage"] = "Report";
    ViewData["Level1"] = "SQLReportAutoSender";
}
<link rel="stylesheet" href="~/lib/codemirror/codemirror.min.css" />
<link rel="stylesheet" href="~/lib/codemirror/3024-night.min.css" />
<div class="container-fluid" id="report-container">
    <input type="hidden" asp-for="@Model.RemoveSubDetailId" />
    <input type="hidden" asp-for="@Model.AsyncAction" />
	<div id="PartialLoaderContainer">
        <partial name="_InputFieldsPartial" model="Model.Report" />
    </div> 
</div>
<div class="row">
    <div class="col-12">
        <p>
            <a asp-page="Index" class="btn btn-secondary">@Localizer["Close"]</a>
            @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Report.Create)).Succeeded)
            {
                <a asp-page="Add" class="btn btn-success">@Localizer["Add"]</a>
            }
            @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Report.Edit)).Succeeded)
            {
                <a asp-page="Edit" asp-route-id="@Model.Report.Id" class="btn btn-primary">@Localizer["Edit"]</a>
            }
            @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Report.Delete)).Succeeded)
            {
                <a asp-page="Delete" asp-route-id="@Model.Report.Id" class="btn btn-danger">@Localizer["Delete"]</a>
            }
        </p>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DisableFieldsPartial" model="@("report-container")" />
<script asp-add-nonce src="~/lib/codemirror/codemirror.min.js"></script>
<script asp-add-nonce src="~/lib/codemirror/sql.min.js"></script>
<script asp-add-nonce src="~/lib/codemirror/closebrackets.min.js"></script>
<script asp-add-nonce src="~/lib/codemirror/closetag.min.js"></script>
<script asp-add-nonce>
    for (let i = 0; i < $('#ReportDetailCount').val(); i++) {
		var editor = CodeMirror.fromTextArea(
		document.getElementById('ReportDetailList_' + i + '_QueryString'), {
			mode: 'sql',
			theme: '3024-night',
			lineNumbers: true,
			autoCloseBrackets: true,
			autoCloseTags: true
		}); 
	}	
</script>
}