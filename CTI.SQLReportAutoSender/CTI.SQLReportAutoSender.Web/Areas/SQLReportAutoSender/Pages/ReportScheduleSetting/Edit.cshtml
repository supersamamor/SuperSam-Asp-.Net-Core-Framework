@page
@model EditModel
@{
    ViewData["Title"] = Localizer["Edit Report Schedule"];
    ViewData["ActiveMainPage"] = "ReportScheduleSetting";
    ViewData["Level1"] = "SQLReportAutoSender";
}
<form id="reportScheduleSetting-form" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>	
	<input type="hidden" asp-for="@Model.RemoveSubDetailId" />
    <input type="hidden" asp-for="@Model.AsyncAction" />
	<div id="PartialLoaderContainer">
        <partial name="_InputFieldsPartial" model="Model.ReportScheduleSetting" />
    </div> 
    <div class="row">
        <div class="col-12">
            <p>
                <a asp-page="Index" class="btn btn-secondary">@Localizer["Cancel"]</a>
                <button type="submit" class="btn btn-success">@Localizer["Save changes"]</button>
            </p>
        </div>
    </div>
</form>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_ValidationScriptsPartial" />

<script asp-add-nonce>
    $(document).ready(function () {
		function InitializeForm()
		{
			$('.DisableSelectDropdown').on('mousedown', function(e) {
				e.preventDefault();
				this.blur();
				window.focus();
			});
			
			$.fn.schedulefrequencySelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/ScheduleFrequency/Index", new { area = "SQLReportAutoSender", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#ScheduleFrequencyId").schedulefrequencySelect();

			$.fn.reportSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/Report/Index", new { area = "SQLReportAutoSender", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#ReportId").reportSelect();

			$.fn.scheduleparameterSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/ScheduleParameter/Index", new { area = "SQLReportAutoSender", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#ScheduleParameterId").scheduleparameterSelect();

		}	
		InitializeForm();
    });
</script>
}