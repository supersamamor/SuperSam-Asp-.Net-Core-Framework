@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Report Schedule List"];
    ViewData["ActiveMainPage"] = "ReportScheduleSetting";
    ViewData["Level1"] = "SQLReportAutoSender";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="reportScheduleSetting" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["ReportId"]
							</th>
							<th>
								@Localizer["ScheduleFrequencyId"]
							</th>
							<th>
								@Localizer["ScheduleParameterId"]
							</th>
							<th>
								@Localizer["Value"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#reportScheduleSetting').DataTable({
            select: false,
            serverSide: true,
            order: [[ 4, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/ReportScheduleSetting/Index", new { area = "SQLReportAutoSender", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "reportId", name: "ReportId", responsivePriority: 1, title: '@Localizer["ReportId"]', searchable: false },
				{ data: "scheduleFrequencyId", name: "ScheduleFrequencyId", responsivePriority: 1, title: '@Localizer["ScheduleFrequencyId"]', searchable: false },
				{ data: "scheduleParameterId", name: "ScheduleParameterId", responsivePriority: 1, title: '@Localizer["ScheduleParameterId"]', searchable: false },
				{ data: "value", name: "Value", responsivePriority: 1, title: '@Localizer["Value"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/ReportScheduleSetting/Details", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btn-secondary btnViewReportScheduleSetting">@Localizer["View"]</a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportScheduleSetting.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ReportScheduleSetting/Edit", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary btnEditReportScheduleSetting">@Localizer["Edit"]</a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportScheduleSetting.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ReportScheduleSetting/Delete", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-danger btnDeleteReportScheduleSetting">@Localizer["Delete"]</a>';
                    </text>
                }
				   
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportScheduleSetting.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/ReportScheduleSetting/Add", new { area = "SQLReportAutoSender" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#reportScheduleSetting tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddReportScheduleSetting",".btnEditReportScheduleSetting",".btnViewReportScheduleSetting",".btnDeleteReportScheduleSetting"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddReportScheduleSetting",".btnEditReportScheduleSetting",".btnViewReportScheduleSetting",".btnDeleteReportScheduleSetting"]);
    });
</script>
}