@page
@model EditModel
@{
    ViewData["Title"] = Localizer["Edit Frequency"];
    ViewData["ActiveMainPage"] = "ScheduleFrequency";
    ViewData["Level1"] = "SQLReportAutoSender";
}
<form id="scheduleFrequency-form" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>	
	<input type="hidden" asp-for="@Model.RemoveSubDetailId" />
    <input type="hidden" asp-for="@Model.AsyncAction" />
	<div id="PartialLoaderContainer">
        <partial name="_InputFieldsPartial" model="Model.ScheduleFrequency" />
    </div> 
    <div class="row">
        <div class="col-12">
            <p>
                <a asp-page="Index" class="btn btn-secondary">@Localizer["Cancel"]</a>
                <button type="submit" class="btn btn-success">@Localizer["Save changes"]</button>
            </p>
        </div>
    </div>
</form>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_ValidationScriptsPartial" />
<partial name="_ScheduleFrequencyParameterSetupListValidationPartial" model="@Model.ScheduleFrequency" />
<partial name="_ReportScheduleSettingListValidationPartial" model="@Model.ScheduleFrequency" />

<script asp-add-nonce>
    $(document).ready(function () {
		function InitializeForm()
		{
			$('.DisableSelectDropdown').on('mousedown', function(e) {
				e.preventDefault();
				this.blur();
				window.focus();
			});
			
			$.InitializeScheduleFrequencyParameterSetupFieldValidations($('#ScheduleFrequencyParameterSetupCount').val());
			$('.RemoveScheduleFrequencyParameterSetupButton ').on( 'click', function () {
				$('#RemoveSubDetailId').val($(this).attr('removeid'));
				$('#AsyncAction').val('RemoveScheduleFrequencyParameterSetup');
				$.triggerPageForm('ChangeFormValue','#PartialLoaderContainer', '#scheduleFrequency-form', InitializeForm);
			});

			$('#btnAddScheduleFrequencyParameterSetup').on( 'click', function () {
				$('#AsyncAction').val('AddScheduleFrequencyParameterSetup');
				$.triggerPageForm('ChangeFormValue','#PartialLoaderContainer', '#scheduleFrequency-form', InitializeForm);
			});

			$.fn.scheduleparameterSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/ScheduleParameter/Index", new { area = "SQLReportAutoSender", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			var scheduleparameterlistdropdowns = document.getElementsByClassName('SelectScheduleParameter');
			for ( var i=0; i< scheduleparameterlistdropdowns.length; i++) {
				$("#ScheduleFrequencyParameterSetupList_"+i+"_ScheduleParameterId").scheduleparameterSelect();
			}

			$.InitializeReportScheduleSettingFieldValidations($('#ReportScheduleSettingCount').val());
			$('.RemoveReportScheduleSettingButton ').on( 'click', function () {
				$('#RemoveSubDetailId').val($(this).attr('removeid'));
				$('#AsyncAction').val('RemoveReportScheduleSetting');
				$.triggerPageForm('ChangeFormValue','#PartialLoaderContainer', '#scheduleFrequency-form', InitializeForm);
			});

			$('#btnAddReportScheduleSetting').on( 'click', function () {
				$('#AsyncAction').val('AddReportScheduleSetting');
				$.triggerPageForm('ChangeFormValue','#PartialLoaderContainer', '#scheduleFrequency-form', InitializeForm);
			});

			$.fn.reportSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/Report/Index", new { area = "SQLReportAutoSender", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			var reportlistdropdowns = document.getElementsByClassName('SelectReport');
			for ( var i=0; i< reportlistdropdowns.length; i++) {
				$("#ReportScheduleSettingList_"+i+"_ReportId").reportSelect();
			}

			$.fn.scheduleparameterSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/ScheduleParameter/Index", new { area = "SQLReportAutoSender", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			var scheduleparameterlistdropdowns = document.getElementsByClassName('SelectScheduleParameter');
			for ( var i=0; i< scheduleparameterlistdropdowns.length; i++) {
				$("#ReportScheduleSettingList_"+i+"_ScheduleParameterId").scheduleparameterSelect();
			}

		}	
		InitializeForm();
    });
</script>
}