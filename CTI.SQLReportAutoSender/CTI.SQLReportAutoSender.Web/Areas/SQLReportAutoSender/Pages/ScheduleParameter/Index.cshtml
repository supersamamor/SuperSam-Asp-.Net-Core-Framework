@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Schedule Parameter List"];
    ViewData["ActiveMainPage"] = "ScheduleParameter";
    ViewData["Level1"] = "SQLReportAutoSender";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="scheduleParameter" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Description"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#scheduleParameter').DataTable({
            select: false,
            serverSide: true,
            order: [[ 1, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/ScheduleParameter/Index", new { area = "SQLReportAutoSender", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "description", name: "Description", responsivePriority: 1, title: '@Localizer["Description"]', searchable: true },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/ScheduleParameter/Details", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btn-secondary btnViewScheduleParameter">@Localizer["View"]</a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ScheduleParameter.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ScheduleParameter/Edit", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary btnEditScheduleParameter">@Localizer["Edit"]</a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ScheduleParameter.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ScheduleParameter/Delete", new { area = "SQLReportAutoSender" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-danger btnDeleteScheduleParameter">@Localizer["Delete"]</a>';
                    </text>
                }
				   
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ScheduleParameter.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/ScheduleParameter/Add", new { area = "SQLReportAutoSender" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#scheduleParameter tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddScheduleParameter",".btnEditScheduleParameter",".btnViewScheduleParameter",".btnDeleteScheduleParameter"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddScheduleParameter",".btnEditScheduleParameter",".btnViewScheduleParameter",".btnDeleteScheduleParameter"]);
    });
</script>
}