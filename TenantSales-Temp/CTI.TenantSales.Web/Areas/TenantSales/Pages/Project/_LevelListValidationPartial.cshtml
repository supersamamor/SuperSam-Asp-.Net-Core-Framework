@using Models
@model ProjectViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializeLevelFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "LevelList[" + idx + "].Name", el: "Input", minlength:0, maxlength:255, isrequired: true, description: "Name"});
				reqs.push({name: "LevelList[" + idx + "].ProjectId", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Project"});
				reqs.push({name: "LevelList[" + idx + "].HasPercentageSalesTenant", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Has Percentage Sales Tenant"});
				reqs.push({name: "LevelList[" + idx + "].IsDisabled", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Disabled"});
				
			}
			let validator = $("#project-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
			} 
		}       
    });
</script>