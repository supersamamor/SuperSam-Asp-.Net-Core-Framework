@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Database Setup List"];
    ViewData["ActiveMainPage"] = "DatabaseConnectionSetup";
    ViewData["Level1"] = "TenantSales";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="databaseConnectionSetup" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Code"]
							</th>
							<th>
								@Localizer["Name"]
							</th>
							<th>
								@Localizer["Database And Server Name"]
							</th>
							  
							<th>
								@Localizer["Status"]
							</th> 							
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#databaseConnectionSetup').DataTable({
            select: false,
            serverSide: true,
            order: [[ 4, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/DatabaseConnectionSetup/Index", new { area = "TenantSales", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "code", name: "Code", responsivePriority: 1, title: '@Localizer["Code"]', searchable: true },
				{ data: "name", name: "Name", responsivePriority: 1, title: '@Localizer["Name"]', searchable: true },
				{ data: "databaseAndServerName", name: "DatabaseAndServerName", responsivePriority: 1, title: '@Localizer["Database And Server Name"]', searchable: true },
				   
				{ data: "statusBadge", name: "StatusBadge", responsivePriority: 1, title: '@Localizer["Approval Status"]', searchable: false, orderable: false },
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/DatabaseConnectionSetup/Details", new { area = "TenantSales" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btn-secondary btnViewDatabaseConnectionSetup">@Localizer["View"]</a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.DatabaseConnectionSetup.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/DatabaseConnectionSetup/Edit", new { area = "TenantSales" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary btnEditDatabaseConnectionSetup">@Localizer["Edit"]</a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.DatabaseConnectionSetup.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/DatabaseConnectionSetup/Delete", new { area = "TenantSales" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-danger btnDeleteDatabaseConnectionSetup">@Localizer["Delete"]</a>';
                    </text>
                }
				@if ((await AuthorizationService.AuthorizeAsync(User, Permission.DatabaseConnectionSetup.Approve)).Succeeded)
				{
					<text>
						url = '@Url.Page("/DatabaseConnectionSetup/Approve", new { area = "TenantSales" })' + '?id=' + row.id;
						button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary">@Localizer["Approve"]</a>';
					</text>
				}   
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.DatabaseConnectionSetup.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/DatabaseConnectionSetup/Add", new { area = "TenantSales" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#databaseConnectionSetup tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddDatabaseConnectionSetup",".btnEditDatabaseConnectionSetup",".btnViewDatabaseConnectionSetup",".btnDeleteDatabaseConnectionSetup"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddDatabaseConnectionSetup",".btnEditDatabaseConnectionSetup",".btnViewDatabaseConnectionSetup",".btnDeleteDatabaseConnectionSetup"]);
    });
</script>
}