@using Models
@model ClassificationViewModel
<input type="hidden" id="CategoryCount" value="@Model?.CategoryList?.Count" />
<div class="row">
    <div class="col-12">
        <div class="card card-outline card-primary">
            <div class="card-header d-flex">
                <div class="card-title flex-grow-1">
                    <button class="btn-field btn btn-block text-left text-black p-0" type="button" data-card-widget="collapse">
                        <h5>@Localizer["Category"]</h5>
                    </button>
                    <button type="button" id="btnAddCategory" class="btn-field btn btn-primary cancel">
                        <i class="fas fa-plus" style="float:left;padding-top: 7px;padding-right: 10px;"></i>
                        <h6 style="float:left;padding-top: 7px;">@Localizer["Add"]</h6>
                    </button>
                    <button class="cancel" type="button" id="btnRemoveCategory" value="RemoveCategory" style="display:none;"></button>
                </div>
                <div class="card-tools">
                    <button type="button" class="btn btn-tool" data-card-widget="collapse">
                        <i class="fas fa-minus"></i>
                    </button>
                </div>
            </div>
            <div class="card-body" style="overflow:auto;">
                <table id="category" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
							<th style="display:none;"></th>
                            <th>
								@Localizer["Name"]
							</th>
							<th>
								@Localizer["Code"]
							</th>
							
                            <th>
                                @Localizer["Action"]
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model?.CategoryList != null)
                        {
                            int counter = 0;
                            foreach (var item in Model.CategoryList)
                            {
                                <tr>
									<td style="display:none;">
                                        <input type="hidden" name="CategoryList[@counter].Id" value="@item.Id" />
                                        <input type="hidden" name="CategoryList[@counter].ClassificationId" value="@item.ClassificationId" />                                      
                                    </td>
                                    <td>
										<input  asp-for="@item.Name" name="CategoryList[@counter].Name" class="form-control" maxlength="80" readonly/>
										<span data-valmsg-for="CategoryList[@counter].Name" asp-validation-for="@item.Name" class="text-danger"></span>
										<input type="hidden" asp-for="@item.Name"  name="CategoryList[@counter].Name"/>
									</td>
									<td>
										<input  asp-for="@item.Code" name="CategoryList[@counter].Code" class="form-control" maxlength="15" readonly/>
										<span data-valmsg-for="CategoryList[@counter].Code" asp-validation-for="@item.Code" class="text-danger"></span>
										<input type="hidden" asp-for="@item.Code"  name="CategoryList[@counter].Code"/>
									</td>
									
                                    <td>
                                        <button type="button" class="btn-field btn btn-danger RemoveCategoryButton cancel" title="@Localizer["Remove Category"]" removeId="@(item.Id)">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </td>
                                </tr>
                                counter++;
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
