@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Tenant List"];
    ViewData["ActiveMainPage"] = "Tenant";
    ViewData["Level1"] = "TenantSales";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="tenant" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Name"]
							</th>
							<th>
								@Localizer["Code"]
							</th>
							<th>
								@Localizer["Date From"]
							</th>
							<th>
								@Localizer["Date To"]
							</th>
							<th>
								@Localizer["Opening"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#tenant').DataTable({
            select: false,
            serverSide: true,
            order: [[ 5, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/Tenant/Index", new { area = "TenantSales", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "name", name: "Name", responsivePriority: 1, title: '@Localizer["Name"]', searchable: true },
				{ data: "code", name: "Code", responsivePriority: 1, title: '@Localizer["Code"]', searchable: true },
				{ data: "dateFrom", name: "DateFrom", responsivePriority: 1, title: '@Localizer["Date From"]', searchable: false },
				{ data: "dateTo", name: "DateTo", responsivePriority: 1, title: '@Localizer["Date To"]', searchable: false },
				{ data: "opening", name: "Opening", responsivePriority: 1, title: '@Localizer["Opening"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/Tenant/Details", new { area = "TenantSales" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btn-secondary btnViewTenant">@Localizer["View"]</a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Tenant.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/Tenant/Edit", new { area = "TenantSales" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary btnEditTenant">@Localizer["Edit"]</a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Tenant.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/Tenant/Delete", new { area = "TenantSales" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-danger btnDeleteTenant">@Localizer["Delete"]</a>';
                    </text>
                }
				   
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Tenant.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/Tenant/Add", new { area = "TenantSales" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#tenant tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddTenant",".btnEditTenant",".btnViewTenant",".btnDeleteTenant"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddTenant",".btnEditTenant",".btnViewTenant",".btnDeleteTenant"]);
    });
</script>
}