@using Models
@model TenantViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializeTenantPOSFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "TenantPOSList[" + idx + "].Code", el: "Input", minlength:0, maxlength:255, isrequired: true, description: "Code"});
				reqs.push({name: "TenantPOSList[" + idx + "].TenantId", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Tenant"});
				reqs.push({name: "TenantPOSList[" + idx + "].SerialNumber", el: "Input", minlength:0, maxlength:255, isrequired: false, description: "Serial Number"});
				reqs.push({name: "TenantPOSList[" + idx + "].IsDisabled", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Disabled"});
				
			}
			let validator = $("#tenant-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
			} 
		}       
    });
</script>