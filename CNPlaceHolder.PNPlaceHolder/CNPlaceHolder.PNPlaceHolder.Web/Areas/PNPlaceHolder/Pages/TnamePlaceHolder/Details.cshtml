@page
@model DetailsModel
@{
    ViewData["Title"] = Localizer["View Tname Description"];
    ViewData["ActiveMainPage"] = "TnamePlaceHolder";
    ViewData["Level1"] = "PNPlaceHolder";
}
<div class="container-fluid" id="tnamePlaceHolder-container">
    <input type="hidden" asp-for="@Model.RemoveSubDetailId" />
    <input type="hidden" asp-for="@Model.AsyncAction" />
    <div id="PartialLoaderContainer">
        <partial name="_InputFieldsPartial" model="Model.TnamePlaceHolder" />
    </div>
</div>
<div id="toolbar-container" class="btn-group" role="group" style="float:right;margin-top:10px;">
	<a asp-page="Index" class="btn btn-secondary" title="@Localizer["Back"]"><i class="fas fa-arrow-left"></i></a>
    @if ((await AuthorizationService.AuthorizeAsync(User, Permission.TnamePlaceHolder.Create)).Succeeded)
    {
        <a asp-page="Add" class="btn btn-success" title="@Localizer["Add"]" style="color:#fff;"><i class="fas fa-plus"></i></a>
    }
    @if ((await AuthorizationService.AuthorizeAsync(User, Permission.TnamePlaceHolder.Edit)).Succeeded)
    {
        <a asp-page="Edit" asp-route-id="@Model.TnamePlaceHolder.Id" class="btn btn-primary" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>
    }
    @if ((await AuthorizationService.AuthorizeAsync(User, Permission.TnamePlaceHolder.Delete)).Succeeded)
    {
        <a asp-page="Delete" asp-route-id="@Model.TnamePlaceHolder.Id" class="btn btn-danger" title="@Localizer["Delete"]"><i class="fas fa-trash"></i></a>
    }
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DisableFieldsPartial" model="@("tnamePlaceHolder-container")" />
}