@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Location List"];
    ViewData["ActiveMainPage"] = "Location";
    ViewData["Level1"] = "LocationApi";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="location" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Barangay Code"]
							</th>
							<th>
								@Localizer["Barangay"]
							</th>
							<th>
								@Localizer["City Code"]
							</th>
							<th>
								@Localizer["City"]
							</th>
							<th>
								@Localizer["Province Code"]
							</th>
							<th>
								@Localizer["Province"]
							</th>
							<th>
								@Localizer["Region Code"]
							</th>
							<th>
								@Localizer["Region"]
							</th>
							<th>
								@Localizer["Full"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#location').DataTable({
            select: false,
            serverSide: true,
            order: [[ 9, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/Location/Index", new { area = "LocationApi", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "barangayCode", name: "BarangayCode", responsivePriority: 1, title: '@Localizer["Barangay Code"]', searchable: false },
				{ data: "barangay", name: "Barangay", responsivePriority: 1, title: '@Localizer["Barangay"]', searchable: true },
				{ data: "cityCode", name: "CityCode", responsivePriority: 1, title: '@Localizer["City Code"]', searchable: false },
				{ data: "city", name: "City", responsivePriority: 1, title: '@Localizer["City"]', searchable: true },
				{ data: "provinceCode", name: "ProvinceCode", responsivePriority: 1, title: '@Localizer["Province Code"]', searchable: false },
				{ data: "province", name: "Province", responsivePriority: 1, title: '@Localizer["Province"]', searchable: true },
				{ data: "regionCode", name: "RegionCode", responsivePriority: 1, title: '@Localizer["Region Code"]', searchable: false },
				{ data: "region", name: "Region", responsivePriority: 1, title: '@Localizer["Region"]', searchable: true },
				{ data: "full", name: "Full", responsivePriority: 1, title: '@Localizer["Full"]', searchable: true },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/Location/Details", new { area = "LocationApi" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewLocation" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Location.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/Location/Edit", new { area = "LocationApi" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditLocation" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Location.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/Location/Delete", new { area = "LocationApi" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeleteLocation" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders" style="color: #DC3545;"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Location.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/Location/Add", new { area = "LocationApi" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#location tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddLocation",".btnEditLocation",".btnViewLocation",".btnDeleteLocation"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddLocation",".btnEditLocation",".btnViewLocation",".btnDeleteLocation"]);

        $('#navbar_search').keyup(function(){
              table.search($(this).val()).draw();
        })
        $(".dataTables_filter").hide();
        $("#navbar_search_container").show();
    });
</script>
}