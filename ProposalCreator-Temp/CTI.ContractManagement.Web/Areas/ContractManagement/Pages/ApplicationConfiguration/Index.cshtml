@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Configuration List"];
    ViewData["ActiveMainPage"] = "ApplicationConfiguration";
    ViewData["Level1"] = "ContractManagement";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="applicationConfiguration" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Logo"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#applicationConfiguration').DataTable({
            select: false,
            serverSide: true,
            order: [[ 1, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/ApplicationConfiguration/Index", new { area = "ContractManagement", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "logo", name: "Logo", responsivePriority: 1, title: '@Localizer["Logo"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/ApplicationConfiguration/Details", new { area = "ContractManagement" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btn-secondary btnViewApplicationConfiguration">@Localizer["View"]</a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ApplicationConfiguration.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ApplicationConfiguration/Edit", new { area = "ContractManagement" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary btnEditApplicationConfiguration">@Localizer["Edit"]</a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ApplicationConfiguration.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ApplicationConfiguration/Delete", new { area = "ContractManagement" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-danger btnDeleteApplicationConfiguration">@Localizer["Delete"]</a>';
                    </text>
                }
				   
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ApplicationConfiguration.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/ApplicationConfiguration/Add", new { area = "ContractManagement" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#applicationConfiguration tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddApplicationConfiguration",".btnEditApplicationConfiguration",".btnViewApplicationConfiguration",".btnDeleteApplicationConfiguration"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddApplicationConfiguration",".btnEditApplicationConfiguration",".btnViewApplicationConfiguration",".btnDeleteApplicationConfiguration"]);
    });
</script>
}