@using Models
@model DeliverableViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializeProjectDeliverableFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "ProjectDeliverableList[" + idx + "].ProjectId", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Project"});
				reqs.push({name: "ProjectDeliverableList[" + idx + "].DeliverableId", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Deliverable"});
				reqs.push({name: "ProjectDeliverableList[" + idx + "].Amount", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Amount"});
				reqs.push({name: "ProjectDeliverableList[" + idx + "].Sequence", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Sequence"});
				
			}
			let validator = $("#deliverable-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
			} 
		}       
    });
</script>