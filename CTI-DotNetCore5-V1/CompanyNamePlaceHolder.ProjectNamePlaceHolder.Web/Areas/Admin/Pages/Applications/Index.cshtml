@page
@model IndexModel

@section Styles {
    <partial name="_DataTableStyles" />
}

@{
    ViewData["ActiveMainPage"] = "Applications";
    ViewData["Title"] = Localizer["Applications List"];
}
<div class="card">
    <div class="card-body">
        <table id="applications" class="table table-hover table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Application!.DisplayName)
                    </th>
                </tr>
            </thead>
        </table>
    </div>
</div>

<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>

<partial name="_FormModalPartial" />

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <partial name="_DataTableScripts" />
    <script asp-add-nonce>
	$(document).ready(function () {
		var table = $('#applications').DataTable({
			serverSide: true,
			order: [[ 0, 'asc' ]],
			ajax:
			{
				url: '@Url.Page("/Applications/Index", new { area = "Admin", handler = "ListAll" })',
				type: "POST",
				headers: {
					RequestVerificationToken:
						$('input:hidden[name="__RequestVerificationToken"]').val()
				},
			},
			columns: [
				{ data: "displayName", name: "DisplayName", title: '@Localizer["Name"]' },
			],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Applications.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/Applications/Add", new { area = "Admin", handler = "Add" })';
                        window.location.href = url;
                    },
				},
            </text>
        }
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Applications.Edit)).Succeeded)
        {
	        <text>
                {
                    extend: 'selectedSingle',
					text: '@Localizer["Edit"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/Applications/Edit", new { area = "Admin", handler = "Details" })' + '&id=' + dt.row({ selected: true }).data().clientId;
                        window.location.href = url;
                    },
				},
	        </text>
        }
                {
                    extend: 'selectedSingle',
					text: '@Localizer["View"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/Applications/View", new { area = "Admin", handler = "Details" })' + '&id=' + dt.row({ selected: true }).data().clientId;
                        window.location.href = url;
                    },
				},
				'excel', 'pdf', 'pageLength',
			],
		});
	});
    </script>
}