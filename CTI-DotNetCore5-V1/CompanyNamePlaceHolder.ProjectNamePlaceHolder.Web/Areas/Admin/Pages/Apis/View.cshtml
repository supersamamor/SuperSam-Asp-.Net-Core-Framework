@page
@model ViewModel

@{
    ViewData["ActiveMainPage"] = "APIs";
    ViewData["Title"] = Localizer["View API"];
}
<div class="card">
    <div class="card-body">
        <div class="form-group row">
            <label asp-for="@Model.Scope!.Name" class="col-md-3 col-form-label"></label>
            <div class="col-md-9">
                <input asp-for="@Model.Scope!.Name" class="form-control" readonly />
            </div>
        </div>
        <div class="form-group row">
            <label asp-for="@Model.Scope!.DisplayName" class="col-md-3 col-form-label"></label>
            <div class="col-md-9">
                <input asp-for="@Model.Scope!.DisplayName" class="form-control" readonly />
                <span asp-validation-for="@Model.Scope!.DisplayName" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group row">
            <label asp-for="@Model.Scope!.Resources" class="col-md-3 col-form-label"></label>
            <div class="col-md-9">
                <input asp-for="@Model.Scope!.Resources" class="form-control" readonly />
                <span asp-validation-for="@Model.Scope!.Resources" class="text-danger"></span>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <a asp-page="Index" class="btn btn-secondary">@Localizer["Close"]</a>
            @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Apis.Edit)).Succeeded)
            {
                <a asp-page="Edit" asp-page-handler="Details" asp-route-name="@Model.Scope!.Name" class="btn btn-primary">@Localizer["Edit"]</a>
            }
            </div>
        </div>
    </div>
</div>