@using Models
@model ReportTableViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializeReportTableJoinParameterFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "ReportTableJoinParameterList[" + idx + "].ReportId", el: "Input", minlength:0, maxlength:50, isrequired: true, description: "Report"});
				reqs.push({name: "ReportTableJoinParameterList[" + idx + "].LogicalOperator", el: "Input", minlength:0, maxlength:50, isrequired: false, description: "Logical Operator"});
				reqs.push({name: "ReportTableJoinParameterList[" + idx + "].TableId", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Table"});
				reqs.push({name: "ReportTableJoinParameterList[" + idx + "].FieldName", el: "Input", minlength:0, maxlength:255, isrequired: true, description: "Field Name"});
				reqs.push({name: "ReportTableJoinParameterList[" + idx + "].JoinFromTableId", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Join from Table"});
				reqs.push({name: "ReportTableJoinParameterList[" + idx + "].JoinFromFieldName", el: "Input", minlength:0, maxlength:255, isrequired: true, description: "Join from FieldName"});
				reqs.push({name: "ReportTableJoinParameterList[" + idx + "].Sequence", el: "Input", minlength:0, maxlength:0, isrequired: true, description: "Sequence"});
				
			}
			let validator = $("#reportTable-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
			} 
		}       
    });
</script>