@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Query Filter List"];
    ViewData["ActiveMainPage"] = "ReportQueryFilter";
    ViewData["Level1"] = "DPI";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="reportQueryFilter" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Report"]
							</th>
							<th>
								@Localizer["Field Name"]
							</th>					
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#reportQueryFilter').DataTable({
            select: false,
            serverSide: true,
            order: [[ 3, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/ReportQueryFilter/Index", new { area = "DPI", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "reportId", name: "ReportId", responsivePriority: 1, title: '@Localizer["Report"]', searchable: false },
				{ data: "fieldName", name: "FieldName", responsivePriority: 1, title: '@Localizer["Field Name"]', searchable: false },
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/ReportQueryFilter/Details", new { area = "DPI" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewReportQueryFilter" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportQueryFilter.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ReportQueryFilter/Edit", new { area = "DPI" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditReportQueryFilter" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportQueryFilter.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ReportQueryFilter/Delete", new { area = "DPI" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeleteReportQueryFilter" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ReportQueryFilter.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/ReportQueryFilter/Add", new { area = "DPI" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#reportQueryFilter tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddReportQueryFilter",".btnEditReportQueryFilter",".btnViewReportQueryFilter",".btnDeleteReportQueryFilter"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddReportQueryFilter",".btnEditReportQueryFilter",".btnViewReportQueryFilter",".btnDeleteReportQueryFilter"]);
        $.EnableNavbarSearch(table);
    });
</script>
}