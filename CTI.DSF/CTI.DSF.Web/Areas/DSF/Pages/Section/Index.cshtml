@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Section List"];
    ViewData["ActiveMainPage"] = "Section";
    ViewData["Level1"] = "DSF";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="section" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Department Code"]
							</th>
							<th>
								@Localizer["Section Code"]
							</th>
							<th>
								@Localizer["Section Name"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#section').DataTable({
            select: false,
            serverSide: true,
            order: [[ 3, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/Section/Index", new { area = "DSF", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "departmentCode", name: "DepartmentCode", responsivePriority: 1, title: '@Localizer["Department Code"]', searchable: true },
				{ data: "sectionCode", name: "SectionCode", responsivePriority: 1, title: '@Localizer["Section Code"]', searchable: true },
				{ data: "sectionName", name: "SectionName", responsivePriority: 1, title: '@Localizer["Section Name"]', searchable: true },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/Section/Details", new { area = "DSF" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewSection" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Section.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/Section/Edit", new { area = "DSF" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditSection" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Section.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/Section/Delete", new { area = "DSF" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeleteSection" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.Section.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/Section/Add", new { area = "DSF" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#section tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddSection",".btnEditSection",".btnViewSection",".btnDeleteSection"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddSection",".btnEditSection",".btnViewSection",".btnDeleteSection"]);
        $.EnableNavbarSearch(table);
    });
</script>
}