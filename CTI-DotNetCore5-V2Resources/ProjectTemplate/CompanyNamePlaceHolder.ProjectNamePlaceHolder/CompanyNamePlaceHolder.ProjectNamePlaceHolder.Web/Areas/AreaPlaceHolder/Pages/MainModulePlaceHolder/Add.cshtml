@page
@model AddModel
@{
    ViewData["Title"] = Localizer["Add MainModulePlaceHolder"];
    ViewData["ActiveMainPage"] = "MainModulePlaceHolder";
    ViewData["Level1"] = "AreaPlaceHolder";
}
<form id="main-form" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row">
        <div class="col-12">
            <div class="card card-outline card-primary">
                <div class="card-header">
                    <h5 class="card-title">@Localizer["MainModulePlaceHolder Info"]</h5>
                    <div class="card-tools">
                        <button type="button" class="btn btn-tool" data-card-widget="collapse">
                            <i class="fas fa-minus"></i>
                        </button>
                    </div>
                </div>
                <div class="card-body">
                    <partial name="_EditMainModulePlaceHolderDetails" model="Model.MainModulePlaceHolder" />
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <p>
                <a asp-page="Index" class="btn btn-secondary">@Localizer["Cancel"]</a>
                <button type="submit" class="btn btn-success">@Localizer["Save changes"]</button>
            </p>
        </div>
    </div>
</form>
@section Scripts {
<partial name="_ValidationScriptsPartial" />
}