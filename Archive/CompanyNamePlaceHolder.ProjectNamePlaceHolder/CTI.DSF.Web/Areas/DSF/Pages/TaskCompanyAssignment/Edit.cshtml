@page
@model EditModel
@{
    ViewData["Title"] = Localizer["Edit Task / Company"];
    ViewData["ActiveMainPage"] = "TaskCompanyAssignment";
    ViewData["Level1"] = "DSF";
}
<form id="taskCompanyAssignment-form" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="@Model.RemoveSubDetailId" />
    <input type="hidden" asp-for="@Model.AsyncAction" />
    <div id="PartialLoaderContainer">
        <partial name="_InputFieldsPartial" model="Model.TaskCompanyAssignment" />
    </div>
    <div id="toolbar-container" class="btn-group" role="group">  
		<a asp-page="Index" id="btnCancelConfirmationModal" class="btn btn-secondary" title="@Localizer["Back"]"><i class="fas fa-arrow-left"></i></a>                
        <button type="button" class="btn btn-success" id="btnConfirmationModal" title="@Localizer["Save changes"]"><i class="fas fa-save"></i></button>
    </div>
</form>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_ValidationScriptsPartial" />
<partial name="_TaskApproverListValidationPartial" model="@Model.TaskCompanyAssignment" />

<script asp-add-nonce>
    $(document).ready(function () {
        function InitializeForm()
        {
			$.bindSaveConfirmationModal('Update', '#btnConfirmationModal','#taskCompanyAssignment-form');
            $.bindCancelConfirmationModal('Cancel', '#btnCancelConfirmationModal','#taskCompanyAssignment-form', 'Do you want to cancel the changes?');
            $('.DisableSelectDropdown').on('mousedown', function(e) {
                e.preventDefault();
                this.blur();
                window.focus();
            });
            
			$.InitializeTaskApproverFieldValidations($('#TaskApproverCount').val());
			$('.RemoveTaskApproverButton ').on( 'click', function () {
				$('#RemoveSubDetailId').val($(this).attr('removeid'));
				$('#AsyncAction').val('RemoveTaskApprover');
				$.triggerPageForm('ChangeFormValue','#PartialLoaderContainer', '#taskCompanyAssignment-form', InitializeForm, true);
			});

			$('#btnAddTaskApprover').on( 'click', function () {
				$('#AsyncAction').val('AddTaskApprover');
				$.triggerPageForm('ChangeFormValue','#PartialLoaderContainer', '#taskCompanyAssignment-form', InitializeForm, true);
			});

			$.fn.departmentSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/Department/Index", new { area = "DSF", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#DepartmentId").departmentSelect();

			$.fn.taskmasterSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/TaskMaster/Index", new { area = "DSF", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#TaskMasterId").taskmasterSelect();

			$.fn.companySelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/Company/Index", new { area = "DSF", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#CompanyId").companySelect();

			$.fn.sectionSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/Section/Index", new { area = "DSF", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#SectionId").sectionSelect();

			$.fn.teamSelect = function() {
				this.filter("select").each(function() {
					$(this).ajaxSelect({
						ajax: {
							url: '@Url.Page("/Team/Index", new { area = "DSF", handler = "Select2Data" })',
						}
					});
				});
				return this;
			};
			$("#TeamId").teamSelect();


        }
        InitializeForm();
    });
</script>
}