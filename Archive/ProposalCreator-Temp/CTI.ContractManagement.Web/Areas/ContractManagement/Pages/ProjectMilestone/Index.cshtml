@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Project Milestone List"];
    ViewData["ActiveMainPage"] = "ProjectMilestone";
    ViewData["Level1"] = "ContractManagement";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="projectMilestone" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Project"]
							</th>
							<th>
								@Localizer["Milestone Stage"]
							</th>
							<th>
								@Localizer["Frequency"]
							</th>
							<th>
								@Localizer["Quantity"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#projectMilestone').DataTable({
            select: false,
            serverSide: true,
            order: [[ 4, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/ProjectMilestone/Index", new { area = "ContractManagement", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "projectId", name: "ProjectId", responsivePriority: 1, title: '@Localizer["Project"]', searchable: false },
				{ data: "milestoneStageId", name: "MilestoneStageId", responsivePriority: 1, title: '@Localizer["Milestone Stage"]', searchable: false },
				{ data: "frequencyId", name: "FrequencyId", responsivePriority: 1, title: '@Localizer["Frequency"]', searchable: false },
				{ data: "frequencyQuantity", name: "FrequencyQuantity", responsivePriority: 1, title: '@Localizer["Quantity"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/ProjectMilestone/Details", new { area = "ContractManagement" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btn-secondary btnViewProjectMilestone">@Localizer["View"]</a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ProjectMilestone.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ProjectMilestone/Edit", new { area = "ContractManagement" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-primary btnEditProjectMilestone">@Localizer["Edit"]</a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ProjectMilestone.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ProjectMilestone/Delete", new { area = "ContractManagement" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btn-danger btnDeleteProjectMilestone">@Localizer["Delete"]</a>';
                    </text>
                }
				   
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ProjectMilestone.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/ProjectMilestone/Add", new { area = "ContractManagement" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#projectMilestone tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddProjectMilestone",".btnEditProjectMilestone",".btnViewProjectMilestone",".btnDeleteProjectMilestone"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddProjectMilestone",".btnEditProjectMilestone",".btnViewProjectMilestone",".btnDeleteProjectMilestone"]);
    });
</script>
}