@{
    var user = await UserManager.GetUserAsync(User);
    var navbarColor = Configuration.GetValue<string>("NavbarColor");
    var appName = Configuration.GetValue<string>("Application");
}
<nav class="main-header navbar navbar-expand navbar-@navbarColor navbar-dark">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/Index" class="nav-link">@appName | @ViewData["Title"]</a>
        </li>
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
        <!-- Navbar Search -->
        <!-- li class="nav-item">
            <a class="nav-link" data-widget="navbar-search" href="#" role="button">
                <i class="fas fa-search"></i>
            </a>
            <div class="navbar-search-block">
                <form class="form-inline">
                    <div class="input-group input-group-sm">
                        <input class="form-control form-control-navbar" type="search" placeholder="Search" aria-label="Search">
                        <div class="input-group-append">
                            <button class="btn btn-navbar" type="submit">
                                <i class="fas fa-search"></i>
                            </button>
                            <button class="btn btn-navbar" type="button" data-widget="navbar-search">
                                <i class="fas fa-times"></i>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </ -->
        <li class="nav-item dropdown user-menu" data-toggle="tooltip" title="User menu">
            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
                <img src="https://ui-avatars.com/api/?background=random&rounded=true&name=@user?.Name" class="user-image img-circle elevation-2" alt="User Image">
                <span class="d-none d-md-inline">@user?.Name</span>
            </a>
            <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                <li class="user-header bg-primary">
                    <img src="https://ui-avatars.com/api/?background=random&rounded=true&name=@user?.Name" class="img-circle elevation-2" alt="User Image">
                    <p>
                        @user?.Name
                        @if (user?.LastUnsucccessfulLogin != null)
                        {
                            <small>@Localizer["Last unsuccessful login"]: @user.LastUnsucccessfulLogin?.ToLocalTime().ToString("MMM dd, yyyy hh:mm:ss tt")</small>
                        }
                    </p>
                </li>
                <!-- Menu Footer-->
                <li class="user-footer">
                    <a href="/Identity/Account/Manage/Index" class="btn btn-default btn-flat">@Localizer["Profile"]</a>
                    <a href="/Identity/Account/Logout" class="btn btn-default btn-flat float-right">@Localizer["Sign out"]</a>
                </li>
            </ul>
        </li>
    </ul>
</nav>
<!-- /.navbar -->
