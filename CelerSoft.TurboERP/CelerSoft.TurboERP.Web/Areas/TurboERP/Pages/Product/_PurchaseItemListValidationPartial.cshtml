@using Models
@model ProductViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializePurchaseItemFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "PurchaseItemList[" + idx + "].Amount", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Amount"});
				reqs.push({name: "PurchaseItemList[" + idx + "].ProductId", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Product"});
				reqs.push({name: "PurchaseItemList[" + idx + "].Quantity", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Quantity", min:1});
				reqs.push({name: "PurchaseItemList[" + idx + "].Remarks", el: "Input", minlength:0, maxlength:400, isrequired: false, description: "Remarks"});
				reqs.push({name: "PurchaseItemList[" + idx + "].SupplierQuotationItemId", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Supplier Quotation Item"});
				
			}
			let validator = $("#product-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
				if(e.min > 0) {el.attr('min', e.min);}   
			} 
		}       
    });
</script>