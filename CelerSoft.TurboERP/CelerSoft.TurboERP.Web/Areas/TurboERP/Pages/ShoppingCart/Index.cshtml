@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Shopping Cart List"];
    ViewData["ActiveMainPage"] = "ShoppingCart";
    ViewData["Level1"] = "TurboERP";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="shoppingCart" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Amount"]
							</th>
							<th>
								@Localizer["Inventory"]
							</th>
							<th>
								@Localizer["Is Check Out"]
							</th>
							<th>
								@Localizer["Quantity"]
							</th>
							<th>
								@Localizer["Shopper Username"]
							</th>
							<th>
								@Localizer["Total Amount"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#shoppingCart').DataTable({
            select: false,
            serverSide: true,
            order: [[ 6, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/ShoppingCart/Index", new { area = "TurboERP", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "amount", name: "Amount", responsivePriority: 1, title: '@Localizer["Amount"]', searchable: false },
				{ data: "inventoryId", name: "InventoryId", responsivePriority: 1, title: '@Localizer["Inventory"]', searchable: false },
				{ data: "isCheckOut", name: "IsCheckOut", responsivePriority: 1, title: '@Localizer["Is Check Out"]', searchable: false },
				{ data: "quantity", name: "Quantity", responsivePriority: 1, title: '@Localizer["Quantity"]', searchable: false },
				{ data: "shopperUsername", name: "ShopperUsername", responsivePriority: 1, title: '@Localizer["Shopper Username"]', searchable: false },
				{ data: "totalAmount", name: "TotalAmount", responsivePriority: 1, title: '@Localizer["Total Amount"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/ShoppingCart/Details", new { area = "TurboERP" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewShoppingCart" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ShoppingCart.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ShoppingCart/Edit", new { area = "TurboERP" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditShoppingCart" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ShoppingCart.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/ShoppingCart/Delete", new { area = "TurboERP" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeleteShoppingCart" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders" style="color: #DC3545;"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.ShoppingCart.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/ShoppingCart/Add", new { area = "TurboERP" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#shoppingCart tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddShoppingCart",".btnEditShoppingCart",".btnViewShoppingCart",".btnDeleteShoppingCart"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddShoppingCart",".btnEditShoppingCart",".btnViewShoppingCart",".btnDeleteShoppingCart"]);

        $('#navbar_search').keyup(function(){
              table.search($(this).val()).draw();
        })
        $(".dataTables_filter").hide();
        $("#navbar_search_container").show();
    });
</script>
}