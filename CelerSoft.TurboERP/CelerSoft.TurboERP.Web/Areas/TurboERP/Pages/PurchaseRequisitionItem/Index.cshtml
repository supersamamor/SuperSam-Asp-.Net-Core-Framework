@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Purchase Requisition Items List"];
    ViewData["ActiveMainPage"] = "PurchaseRequisitionItem";
    ViewData["Level1"] = "TurboERP";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="purchaseRequisitionItem" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Purchase Requisition"]
							</th>
							<th>
								@Localizer["Product"]
							</th>
							<th>
								@Localizer["Quantity"]
							</th>
							<th>
								@Localizer["Remarks"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#purchaseRequisitionItem').DataTable({
            select: false,
            serverSide: true,
            order: [[ 4, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/PurchaseRequisitionItem/Index", new { area = "TurboERP", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "purchaseRequisitionId", name: "PurchaseRequisitionId", responsivePriority: 1, title: '@Localizer["Purchase Requisition"]', searchable: false },
				{ data: "productId", name: "ProductId", responsivePriority: 1, title: '@Localizer["Product"]', searchable: false },
				{ data: "quantity", name: "Quantity", responsivePriority: 1, title: '@Localizer["Quantity"]', searchable: false },
				{ data: "remarks", name: "Remarks", responsivePriority: 1, title: '@Localizer["Remarks"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/PurchaseRequisitionItem/Details", new { area = "TurboERP" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewPurchaseRequisitionItem" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.PurchaseRequisitionItem.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/PurchaseRequisitionItem/Edit", new { area = "TurboERP" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditPurchaseRequisitionItem" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.PurchaseRequisitionItem.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/PurchaseRequisitionItem/Delete", new { area = "TurboERP" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeletePurchaseRequisitionItem" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders" style="color: #DC3545;"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.PurchaseRequisitionItem.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/PurchaseRequisitionItem/Add", new { area = "TurboERP" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#purchaseRequisitionItem tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddPurchaseRequisitionItem",".btnEditPurchaseRequisitionItem",".btnViewPurchaseRequisitionItem",".btnDeletePurchaseRequisitionItem"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddPurchaseRequisitionItem",".btnEditPurchaseRequisitionItem",".btnViewPurchaseRequisitionItem",".btnDeletePurchaseRequisitionItem"]);

        $('#navbar_search').keyup(function(){
              table.search($(this).val()).draw();
        })
        $(".dataTables_filter").hide();
        $("#navbar_search_container").show();
    });
</script>
}