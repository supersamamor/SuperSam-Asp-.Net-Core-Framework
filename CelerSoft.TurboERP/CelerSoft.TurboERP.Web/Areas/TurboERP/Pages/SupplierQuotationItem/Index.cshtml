@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Supplier Quotation Items List"];
    ViewData["ActiveMainPage"] = "SupplierQuotationItem";
    ViewData["Level1"] = "TurboERP";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="supplierQuotationItem" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Supplier Quotation"]
							</th>
							<th>
								@Localizer["Product"]
							</th>
							<th>
								@Localizer["Quantity"]
							</th>
							<th>
								@Localizer["Amount"]
							</th>
							<th>
								@Localizer["Remarks"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#supplierQuotationItem').DataTable({
            select: false,
            serverSide: true,
            order: [[ 5, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/SupplierQuotationItem/Index", new { area = "TurboERP", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "supplierQuotationId", name: "SupplierQuotationId", responsivePriority: 1, title: '@Localizer["Supplier Quotation"]', searchable: false },
				{ data: "productId", name: "ProductId", responsivePriority: 1, title: '@Localizer["Product"]', searchable: false },
				{ data: "quantity", name: "Quantity", responsivePriority: 1, title: '@Localizer["Quantity"]', searchable: false },
				{ data: "amount", name: "Amount", responsivePriority: 1, title: '@Localizer["Amount"]', searchable: false },
				{ data: "remarks", name: "Remarks", responsivePriority: 1, title: '@Localizer["Remarks"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/SupplierQuotationItem/Details", new { area = "TurboERP" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewSupplierQuotationItem" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.SupplierQuotationItem.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/SupplierQuotationItem/Edit", new { area = "TurboERP" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditSupplierQuotationItem" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.SupplierQuotationItem.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/SupplierQuotationItem/Delete", new { area = "TurboERP" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeleteSupplierQuotationItem" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders" style="color: #DC3545;"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.SupplierQuotationItem.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/SupplierQuotationItem/Add", new { area = "TurboERP" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#supplierQuotationItem tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddSupplierQuotationItem",".btnEditSupplierQuotationItem",".btnViewSupplierQuotationItem",".btnDeleteSupplierQuotationItem"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddSupplierQuotationItem",".btnEditSupplierQuotationItem",".btnViewSupplierQuotationItem",".btnDeleteSupplierQuotationItem"]);

        $('#navbar_search').keyup(function(){
              table.search($(this).val()).draw();
        })
        $(".dataTables_filter").hide();
        $("#navbar_search_container").show();
    });
</script>
}