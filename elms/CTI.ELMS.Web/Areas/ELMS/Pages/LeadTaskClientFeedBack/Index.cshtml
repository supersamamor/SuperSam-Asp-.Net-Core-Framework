@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Lead Task / Client Feedback Setup List"];
    ViewData["ActiveMainPage"] = "LeadTaskClientFeedBack";
    ViewData["Level1"] = "ELMS";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="leadTaskClientFeedBack" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Lead Task"]
							</th>
							<th>
								@Localizer["Client Feedback"]
							</th>
							<th>
								@Localizer["Activity Status"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#leadTaskClientFeedBack').DataTable({
            select: false,
            serverSide: true,
            order: [[ 3, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/LeadTaskClientFeedBack/Index", new { area = "ELMS", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "leadTaskId", name: "LeadTaskId", responsivePriority: 1, title: '@Localizer["Lead Task"]', searchable: false },
				{ data: "clientFeedbackId", name: "ClientFeedbackId", responsivePriority: 1, title: '@Localizer["Client Feedback"]', searchable: false },
				{ data: "activityStatus", name: "ActivityStatus", responsivePriority: 1, title: '@Localizer["Activity Status"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/LeadTaskClientFeedBack/Details", new { area = "ELMS" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewLeadTaskClientFeedBack" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.LeadTaskClientFeedBack.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/LeadTaskClientFeedBack/Edit", new { area = "ELMS" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditLeadTaskClientFeedBack" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.LeadTaskClientFeedBack.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/LeadTaskClientFeedBack/Delete", new { area = "ELMS" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeleteLeadTaskClientFeedBack" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders" style="color: #DC3545;"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.LeadTaskClientFeedBack.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/LeadTaskClientFeedBack/Add", new { area = "ELMS" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#leadTaskClientFeedBack tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddLeadTaskClientFeedBack",".btnEditLeadTaskClientFeedBack",".btnViewLeadTaskClientFeedBack",".btnDeleteLeadTaskClientFeedBack"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddLeadTaskClientFeedBack",".btnEditLeadTaskClientFeedBack",".btnViewLeadTaskClientFeedBack",".btnDeleteLeadTaskClientFeedBack"]);

        $('#navbar_search').keyup(function(){
              table.search($(this).val()).draw();
        })
        $(".dataTables_filter").hide();
        $("#navbar_search_container").show();
    });
</script>
}