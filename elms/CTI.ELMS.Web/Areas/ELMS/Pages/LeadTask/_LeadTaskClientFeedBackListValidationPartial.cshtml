@using Models
@model LeadTaskViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializeLeadTaskClientFeedBackFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "LeadTaskClientFeedBackList[" + idx + "].LeadTaskId", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Lead Task"});
				reqs.push({name: "LeadTaskClientFeedBackList[" + idx + "].ClientFeedbackId", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Client Feedback"});
				reqs.push({name: "LeadTaskClientFeedBackList[" + idx + "].ActivityStatus", el: "Input", minlength:0, maxlength:25, isrequired: false, description: "Activity Status"});
				
			}
			let validator = $("#leadTask-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
			} 
		}       
    });
</script>