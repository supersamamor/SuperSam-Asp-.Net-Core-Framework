@using Models
@model LeadTaskViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializeActivityHistoryFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "ActivityHistoryList[" + idx + "].ActivityID", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Activity"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].LeadTaskId", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Lead Task"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].ActivityDate", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Activity Date"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].ClientFeedbackId", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Client Feedback"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].NextStepId", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Next Step"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].TargetDate", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Target Date"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].ActivityRemarks", el: "Input", minlength:0, maxlength:500, isrequired: false, description: "Activity Remarks"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].PCTDate", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "PCT Date"});
				reqs.push({name: "ActivityHistoryList[" + idx + "].UnitsInformation", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Units Information"});
				
			}
			let validator = $("#leadTask-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
			} 
		}       
    });
</script>