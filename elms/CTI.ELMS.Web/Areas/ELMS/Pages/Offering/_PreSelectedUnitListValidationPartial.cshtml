@using Models
@model OfferingViewModel
<script asp-add-nonce>
    $(document).ready(function () { 
		$.InitializePreSelectedUnitFieldValidations = function(count)
        {
			let reqs = [];			
			for (let idx = 0; idx < count; idx++) {
				reqs.push({name: "PreSelectedUnitList[" + idx + "].OfferingID", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Offering"});
				reqs.push({name: "PreSelectedUnitList[" + idx + "].UnitID", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Unit"});
				reqs.push({name: "PreSelectedUnitList[" + idx + "].LotBudget", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Lot Budget"});
				reqs.push({name: "PreSelectedUnitList[" + idx + "].LotArea", el: "Input", minlength:0, maxlength:0, isrequired: false, description: "Lot Area"});
				
			}
			let validator = $("#offering-form").validate();
			for (let j in reqs)
			{
				let e = reqs[j];
				let el = $(`*[name="${e.name}"]`);
				el.rules("add", {
					required: {                
						depends: function(element) {
							return e.isrequired;
						}
					},
					messages: {
						required: (e.description === '') ? 'this field is required' : `'${e.description}' is required`,
					},
				});
				if(e.isrequired == true)
				{
					let labelElement = $(`label[name="Label.${e.name}"]`);
					$(labelElement).addClass("fieldrequired");
				}
				if(e.maxlength > 0) {el.attr('maxlength', e.maxlength);}
				if(e.minlength > 0) {el.attr('minlength', e.minlength);}   
			} 
		}       
    });
</script>