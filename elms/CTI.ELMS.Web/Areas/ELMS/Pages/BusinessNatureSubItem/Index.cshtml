@page
@model IndexModel

@section Styles {
<partial name="_DataTableStyles" />
}
@{
    ViewData["Title"] = Localizer["Business Nature / Sub Item Setup List"];
    ViewData["ActiveMainPage"] = "BusinessNatureSubItem";
    ViewData["Level1"] = "ELMS";
}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <table id="businessNatureSubItem" class="table table-hover table-bordered table-striped responsive nowrap" width="100%">
                    <thead>
                        <tr>
                            <th>
								@Localizer["Business Nature Sub Item Name"]
							</th>
							<th>
								@Localizer["Business Nature"]
							</th>
							<th>
								@Localizer["Is Disabled"]
							</th>
							<th>
								@Localizer["Business Nature Sub Item Code"]
							</th>
							  
														
                            <th>
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
<a id="back-to-top" href="#" class="btn btn-primary back-to-top" role="button" aria-label="Scroll to top">
    <i class="fas fa-chevron-up"></i>
</a>
@section Scripts {
<partial name="_DataTableScripts" />
<script asp-add-nonce>
    $(document).ready(function () {
        var table = $('#businessNatureSubItem').DataTable({
            select: false,
            serverSide: true,
            order: [[ 4, 'desc' ]],
            ajax:
            {
                url: '@Url.Page("/BusinessNatureSubItem/Index", new { area = "ELMS", handler = "ListAll" })',
                type: "POST",
                headers: {
                    RequestVerificationToken:
                        $('input:hidden[name="__RequestVerificationToken"]').val()
                },
            },
			initComplete: function( settings, json ) {
                BindTableRowCollapseEvent();
            },
			columns: [
                { data: "businessNatureSubItemName", name: "BusinessNatureSubItemName", responsivePriority: 1, title: '@Localizer["Business Nature Sub Item Name"]', searchable: false },
				{ data: "businessNatureID", name: "BusinessNatureID", responsivePriority: 1, title: '@Localizer["Business Nature"]', searchable: false },
				{ data: "isDisabled", name: "IsDisabled", responsivePriority: 1, title: '@Localizer["Is Disabled"]', searchable: false },
				{ data: "businessNatureSubItemCode", name: "BusinessNatureSubItemCode", responsivePriority: 1, title: '@Localizer["Business Nature Sub Item Code"]', searchable: false },
				   
				
                {
                    data: null,
                    searchable: false,
                    orderable: false,
                    responsivePriority: 2,
                    width: "10%",
                    render: function (data, type, row, meta) {
                        var url = '@Url.Page("/BusinessNatureSubItem/Details", new { area = "ELMS" })' + '?id=' + row.id;
                        var button = '<a href="' + url + '" class="btn btnMaterial btn-flat primary semicircle btnViewBusinessNatureSubItem" title="@Localizer["View"]"><i class="far fa-eye"></i></a>';
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.BusinessNatureSubItem.Edit)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/BusinessNatureSubItem/Edit", new { area = "ELMS" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat success semicircle btnEditBusinessNatureSubItem" title="@Localizer["Edit"]"><i class="fas fa-pen"></i></a>';
                    </text>
                }
                @if ((await AuthorizationService.AuthorizeAsync(User, Permission.BusinessNatureSubItem.Delete)).Succeeded)
                {
                    <text>
                        url = '@Url.Page("/BusinessNatureSubItem/Delete", new { area = "ELMS" })' + '?id=' + row.id;
                        button =  button + '&nbsp;<a href="' + url + '" class="btn btnMaterial btn-flat accent semicircle btnDeleteBusinessNatureSubItem" title="@Localizer["Delete"]"><i class="fas fa-trash btnNoBorders" style="color: #DC3545;"></i></a>';
                    </text>
                }
				  
                        return button;
                    },
                },
                { data: "lastModifiedDate", name: "LastModifiedDate", title: '@Localizer["Last Modified Date"]', visible: false, searchable: false },
                { data: "id", name: "Id", visible: false },
            ],
            buttons: [
        @if ((await AuthorizationService.AuthorizeAsync(User, Permission.BusinessNatureSubItem.Create)).Succeeded)
        {
            <text>
                {
                    text: '@Localizer["Add"]',
                    action: function (e, dt, button, config) {
                        var url = '@Url.Page("/BusinessNatureSubItem/Add", new { area = "ELMS" })';
                        window.location.href = url;
                    },
                    className: 'btn btn-success text-light',
                },
            </text>
        }
                'excel', 'pdf', 'pageLength',
            ],
        });
        var debounce = new $.fn.dataTable.Debounce(table); // Add delay to search

        $('.btn-table_filter').on('click', function(e) {
            table.ajax.reload(null, false);
        });
		function BindTableRowCollapseEvent()
        {
            $('#businessNatureSubItem tbody').on('click', 'td.dtr-control', function () {
                $.showAjaxLoaderOnClick([".btnAddBusinessNatureSubItem",".btnEditBusinessNatureSubItem",".btnViewBusinessNatureSubItem",".btnDeleteBusinessNatureSubItem"]);
            });
        }
        $.showAjaxLoaderOnClick([".btnAddBusinessNatureSubItem",".btnEditBusinessNatureSubItem",".btnViewBusinessNatureSubItem",".btnDeleteBusinessNatureSubItem"]);

        $('#navbar_search').keyup(function(){
              table.search($(this).val()).draw();
        })
        $(".dataTables_filter").hide();
        $("#navbar_search_container").show();
    });
</script>
}